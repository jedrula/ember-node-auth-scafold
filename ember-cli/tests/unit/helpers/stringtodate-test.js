import { stringtodate } from 'project-andrzej-swaton-cli/helpers/stringtodate';
import { module, test } from 'qunit';

module('Unit | Helper | stringtodate');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stringtodate([42]);
  assert.ok(result);
});
