{{expense-form
  persist=(action "edit" model)
  amount=(readonly model.amount)
  description=(readonly model.description)
  currency=(readonly model.currency)
  title=(readonly model.title)
  href=(readonly model.href)
}}
