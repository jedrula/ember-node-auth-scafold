{{user-expenses-print
  expenses=model.expenses
  user=model.user
  minDate=(stringtodate parentctrl.minDate)
  maxDate=(stringtodate parentctrl.maxDate)
  description=parentctrl.description
  amount=parentctrl.amount
}}
